import { Button, Center } from '@chakra-ui/react';

const Home = () => {
  return (
    <Center sx={{ minHeight: '100vh' }} bg="Background">
      <Button colorScheme="teal">Home Page</Button>
    </Center>
  );
};

export default Home;
